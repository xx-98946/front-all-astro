---
import Layout from "@/layouts/Layout.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog")).sort(
  (a, b) => b.data.发布时间.valueOf() - a.data.发布时间.valueOf(),
);
---

<Layout title="博客列表">
  <section>
    <ul>
      {
        posts.map((post) => (
          <li>
            <a href={`/blog/${post.id}/`}>
              <img src={post.data.图片} class="w-10" />
              <h4 class="title">{post.data.标题}</h4>
            </a>
          </li>
        ))
      }
    </ul>
  </section>
</Layout>
